# GameVisionLab 1.0.3（Build 5）

本版本重点简化配置管理与复用流程，并同步当前最新的内置玩法配置。

## 主要更新

### 配置复制与复用

- 一键复制整个玩法时，完整复制其私有界面、点击动作和流程节点，并自动重建内部引用。
- 通用战斗、通用弹窗等共享配置继续引用原配置，不生成重复副本。
- 界面配置和点击配置新增单项复制；副本名称自动增加递增标记，便于在全局菜单中区分。

### 内置配置与用户配置

- 新安装用户首次启动时自动获得随 App 发布的内置配置。
- 配置管理新增“重新导入内置配置”，可恢复当前 App 自带配置，同时保留用户新增或复制的配置。
- 后续启动不再自动补回用户主动删除的内置项目。
- 内置配置和用户配置均可编辑、复制和删除，来源仅用于导入时区分。

### 配置中心体验

- “配置检查”升级为“配置管理”，集中放置全局配置操作。
- 界面、点击和流程节点统一在详情页右上角显示核查状态，可直接标记为已核查。
- 悬浮窗统一显示玩法名称，移除不再需要的隐藏流程名称。
- 完善整玩法复制时的 OCR 区域、文字匹配、点击参数和核查状态继承。

### 配置更新

- 补充英杰试炼－秘境的重置界面、点击动作和流程节点。
- 内置配置更新为 23 个界面、19 个点击动作、7 套流程和 41 个流程节点。

## 安装说明

1. 下载并解压 `GameVisionLab-1.0.3-macOS-arm64.zip`。
2. 将 `GameVisionLab.app` 放入“应用程序”文件夹并覆盖旧版本。
3. 如 macOS 阻止打开，请在“系统设置 → 隐私与安全性”中确认允许。
4. 按需要授予“屏幕与系统录音”和“辅助功能”权限。

当前安装包采用临时签名，尚未经过 Apple 公证。

## 使用提醒

建议先使用“只观察”确认窗口和界面识别结果，再使用“单步点击”验证动作，确认无误后再开启连续运行。本工具不是网易或《阴阳师》的官方产品，自动化操作可能产生误点击、资源损失或账号风险，请自行评估并承担使用风险。

## 下载文件

- `GameVisionLab-1.0.3-macOS-arm64.zip`
- `GameVisionLab-1.0.3-macOS-arm64.zip.sha256`

SHA-256：

```text
8be109ee490d7dbe09dc6e13a6c49709ffc4ceb92bc8e2d065590a4df1668ea7
```
