# GameVisionLab 2.1.0 安装与配置说明

当前测试包使用临时签名，尚未使用 Apple Developer ID 公证。

## 安装

1. 下载并解压 `GameVisionLab-2.1.0-macOS-arm64.zip`。
2. 将 `GameVisionLab.app` 拖入“应用程序”，覆盖旧版本。
3. 第一次尝试打开后，如果 macOS 阻止启动，进入“系统设置 → 隐私与安全性”，点击“仍要打开”。
4. 按提示授予屏幕录制和辅助功能权限，然后完全退出并重新打开 App。
5. 选择正确的游戏窗口，先使用“只观察”模式，核对锁定窗口、当前界面、目标区域和预计下一步行为。
6. 观察结果正确后，使用“单步点击”执行一个动作，确认实际点击位置和页面变化符合预期。
7. 观察和单步均正常后，再开始连续运行。

## 2.1.0 百鬼夜行

2.1.0 继续使用 `Hyakki Vision` 处理百鬼夜行动态目标，并优化了进入“百鬼进行中”状态时的确认延迟：

- 百鬼入口、开始、完成、奖励等页面仍可使用普通 OCR / 点击配置。
- “百鬼进行中”不要求固定 OCR 文字，由内置 Core ML Detector 检测目标并结合向左运动证据确认进入。
- 2.1.0 减少了一次不必要的连续运动确认；仍保留真实运动判断，降低进入延迟的同时避免仅凭静态角色图进入投豆状态。
- 进入百鬼后悬浮监视器会切换为百鬼实时预览，显示 Track、Focus、AIM、投豆次数与延迟。
- 投豆动作支持最短/最长冷却范围，并在每次投豆后重新随机下一次冷却时间。
- 百鬼结束后仍需要识别到流程允许的下一界面，才会继续执行后续结算或返回流程。

首次使用百鬼流程时，建议先使用“只观察”确认 Detector、Track、Focus 和 AIM 位置，再启用真实点击。

## 配置中心：出现识别或点击问题时怎么调

GameVisionLab 的自动化不是单纯“看到按钮就点击”，而是按 **界面识别 → 点击动作 → 流程约束** 三层配置运行。多数识别偏差、按钮位置变化或流程跳转问题，都可以先在配置中心中调整，而不需要修改程序。

### 1. 界面配置：决定“现在是什么页面”

界面配置主要用于 OCR 页面识别：

- **必须文字**：用于确认当前页面；如果这些关键文字长期识别不到，应优先检查文字内容和 OCR 区域。
- **可选文字**：可作为额外识别证据，但不应替代真正稳定的关键文字。
- **排除文字**：用于避免相似页面误判；如果两个页面都有相同标题，可以用排除文字进一步区分。
- **OCR 识别区域**：限制文字识别范围。游戏 UI 改版、窗口比例变化或文字位置移动时，可以重新调整区域。
- **特殊视觉识别**：像“百鬼进行中”这类没有稳定文字的动态页面，可以使用 Hyakki Vision，而不是强行依赖 OCR。

如果监视器显示“未识别”或经常认成错误页面，优先处理这一层。

### 2. 点击配置：决定“在这个页面点哪里”

点击配置只会在当前界面已经确认后使用。可以根据页面特点配置固定区域、OCR 锚点、圆形目标或 Hyakki Vision 等不同定位方式。

出现以下问题时，应检查点击配置：

- 识别页面正确，但点击位置偏了：检查 **搜索区域 / 点击区域**。
- 按钮位置会变化：优先使用能跟随文字或目标位置的定位方式，不要依赖绝对屏幕坐标。
- 点击后没有进入下一页：先用“单步点击”确认实际点击位置，再检查按钮是否真的被游戏接受。
- 百鬼投豆位置或目标选择异常：先观察 Track、Focus 和 AIM，再调整百鬼相关参数，不要直接提高点击频率掩盖识别问题。

### 3. 流程配置：决定“识别后允许去哪里、做什么”

流程配置负责把各个界面串起来。每个流程节点可以规定当前界面的行为、稳定帧数、允许的下一界面、超时和有限重试等规则。

因此即使 OCR 识别到某个页面，如果它不符合当前流程允许的跳转关系，App 也不会随意继续点击。修改流程时重点检查：

- 当前节点对应的界面是否正确；
- 当前节点应该 **等待、执行动作还是停止**；
- `允许的下一界面` 是否覆盖真实游戏流程；
- 页面动画较多时，稳定帧数是否太低；
- 页面变化较慢时，超时时间是否太短；
- 重试次数是否合理，避免错误状态下无限点击。

### 推荐调试顺序

出现问题时建议按这个顺序排查：

1. **只观察**：先确认当前界面识别是否正确。
2. 如果界面错误，先改 **界面配置**；不要先改点击位置。
3. 界面正确但目标或按钮位置错误，再改 **点击配置**。
4. 界面和点击都正确，但下一步没有继续或跳转不符合预期，再检查 **流程配置**。
5. 修改后先重新使用“只观察”，再测试“单步点击”，最后才恢复连续运行。

内置配置可以修改、复制和删除。如果调整后希望恢复当前版本自带配置，可在“配置管理”选择“重新导入内置配置”；用户新增和复制的配置会保留。对于改动较大的调试，建议先复制一份配置再修改。

## 后台模式建议

1. **使用后台模式前请关闭 macOS「台前调度（Stage Manager）」**。开启台前调度时，游戏窗口的舞台切换、层级和坐标状态可能发生变化，百鬼夜行等实时模式可能出现点击位置异常。
2. 后台模式不移动真实鼠标；虚拟光标仅用于显示实际发送到目标窗口的点击位置。
3. 保持目标窗口打开且不最小化，建议与 GameVisionLab 位于同一桌面。
4. 进入 Mission Control 或切换桌面时，App 会对暂时不可见的窗口等待恢复；超时仍未恢复时会安全停止。
5. macOS 版 iPad App 对激活事件更敏感，连续运行时避免频繁切换窗口或空间。
6. 如果页面没有在点击后变化，请用 `Esc` 停止，检查屏幕录制与辅助功能权限，再用单步模式复核。
7. `Esc` 仍是紧急停止键；任务取消后不会继续启动新的点击动作。

首次安装包含发布时脱敏并核查过的界面、点击、流程及特殊识别配置。2.1.0 的 Release 包同时包含百鬼夜行运行需要的 Core ML Detector；不包含开发者的原始截图、标注库、日志或窗口历史。

App 每天最多检查一次 GameVisionLab 的 GitHub Release；发现新版本时只提示前往下载，不会自动安装或替换 App。

支持范围：Apple Silicon Mac；macOS 14.0 或更高版本。

不同窗口尺寸、游戏语言、UI 版本和画质仍可能影响识别，首次使用必须核查。
