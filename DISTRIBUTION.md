# GameVisionLab 1.1.1 安装说明

当前测试包使用临时签名，尚未使用 Apple Developer ID 公证。

1. 下载并解压 `GameVisionLab-1.1.1-macOS-arm64.zip`。
2. 将 `GameVisionLab.app` 拖入“应用程序”。
3. 第一次尝试打开后，如果 macOS 阻止启动，进入“系统设置 → 隐私与安全性”，点击“仍要打开”。
4. 按提示授予屏幕录制和辅助功能权限，然后完全退出并重新打开 App。
5. 选择正确的游戏窗口，先使用“只观察”模式，核对锁定窗口、当前界面、OCR 文字、目标区域和预计下一步行为。
6. 观察结果正确后，使用“单步点击”执行一个动作，确认实际点击位置和页面变化符合预期。
7. 观察和单步均正常后，再开始连续运行。

## 后台模式建议

1. 后台模式不移动真实鼠标；虚拟光标仅用于显示实际发送到目标窗口的点击位置。
2. 保持目标窗口打开且不最小化，建议与 GameVisionLab 位于同一桌面。
3. 进入 Mission Control 或切换桌面时，App 会对暂时不可见的窗口等待约 10 秒；超时仍未恢复时会安全停止。
4. macOS 版 iPad App 对激活事件更敏感，连续运行时避免频繁切换窗口或空间。
5. 如果页面没有在点击后变化，请用 `Esc` 停止，检查屏幕录制与辅助功能权限，再用单步模式复核。
6. 1.1.1 已进一步加强取消与拖拽释放安全；任务停止后如仍看到异常状态，请完全退出 App 后重新打开再测试。

首次安装已经包含开发者根据实际采集画面整理并核查过的探索、结界突破、御魂、觉醒、秘闻、通用战斗和英杰试炼－秘境流程。原始截图不在安装包内，运行依赖的文字、相对区域、动作和流程规则仍然存在。

App 每天最多检查一次 GameVisionLab 的 GitHub Release；发现新版本时只提示前往下载，不会自动安装或替换 App。

内置配置可以修改、复制和删除。如果需要恢复当前版本自带配置，可在“配置管理”选择“重新导入内置配置”；用户新增和复制的配置会保留。

支持范围：Apple Silicon Mac；macOS 14.0 或更高版本。

安装包内置脱敏的界面、点击和流程配置，不包含开发者的截图、标注库、日志或窗口历史。不同窗口尺寸、游戏语言、UI 版本和画质仍可能影响识别，首次使用必须核查。
