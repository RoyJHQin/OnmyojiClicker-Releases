# GameVisionLab 1.1.0（Build 6）

本版本重点新增窗口定向的后台模式，并完善 macOS 版 iPad App 的激活稳定性、窗口恢复和自动玩法修正。

## 主要更新

### 后台模式

- 点击和拖动可以发送到指定游戏窗口，不需要移动真实鼠标。
- 运行时保持用户当前看到的前台 App，不把游戏窗口强制抢到视觉前台。
- 为原前台 App 和目标 App 建立 PID 级 Event Tap，并向目标发送 AppKit 激活会话。
- 会话结束时先拆除 Event Tap，再向目标和原窗口发送对应的失活/激活通知。
- 可选虚拟光标显示后台点击位置，仅位于目标窗口上方，不会改变真实光标位置。

### 稳定性与自动恢复

- 当目标 App 失去内部激活或 Event Tap 失效时，不再继续使用过期会话，而是基于当前前台窗口重建会话。
- 修复 Event Tap 将普通鼠标/键盘事件当成 AppKit 激活事件处理时的崩溃。
- 四指上滑进入 Mission Control 时，目标窗口的瞬时不可见不再被误判为立即中止；App 会先短暂等待恢复。
- 自动模式从战斗中启动时，可在战斗结束后根据返回界面修正为通用战斗，不再被探索流程遗留的过渡约束中止。

## 后台模式 Tips

1. 首次使用先选择正确游戏窗口，用“只观察”核对识别结果。
2. 再用“单步点击”确认后台点击位置和页面变化，确认无误后才连续运行。
3. 虚拟光标是点击位置提示，不是系统真实鼠标；不需要时可关闭。
4. 保持目标 App 运行且窗口不最小化、不关闭，建议放在当前桌面。
5. Mission Control 或桌面切换会让窗口暂时不可捕获；App 会等待约 10 秒，长时间不恢复才停止。
6. macOS 版 iPad App 对激活状态更敏感，建议避免在关键过渡阶段快速反复切换窗口。
7. 如果点击已发送但画面没有变化，用 `Esc` 停止，检查屏幕录制和辅助功能权限，再从单步验证开始。

## 内置配置

- 22 个界面定义
- 19 个已核查点击动作
- 7 套流程
- 41 个已核查流程节点

## 安装说明

1. 下载并解压 `GameVisionLab-1.1.0-macOS-arm64.zip`。
2. 将 `GameVisionLab.app` 放入“应用程序”并覆盖旧版。
3. 如 macOS 阻止打开，请在“系统设置 → 隐私与安全性”中选择“仍要打开”。
4. 授予屏幕录制和辅助功能权限后，完全退出并重新打开 App。

当前安装包为临时签名，尚未经过 Apple 公证。

## 下载文件

- `GameVisionLab-1.1.0-macOS-arm64.zip`
- `GameVisionLab-1.1.0-macOS-arm64.zip.sha256`

SHA-256：

```text
2962872eacf569599f43775185d6b415767432341a586a136b0adb2b5342bcf9
```
