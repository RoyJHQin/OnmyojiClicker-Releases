# GameVisionLab 1.1.1（Build 6）

1.1.1 是基于 1.1.0 的输入安全修复版本，重点降低后台激活、任务取消和拖拽中断时产生额外输入或鼠标未释放的风险。

## 主要更新

### 后台激活更安全

- 后台窗口激活引导不再发送合成 `leftMouseDown/leftMouseUp`。
- 改为向目标窗口发送非点击的定向 `mouseMoved` 事件，保留窗口定向激活路径，同时避免激活过程本身带来点击语义。
- 后台模式依旧不会移动系统真实鼠标；虚拟光标仍只作为目标位置提示。

### 取消与紧急停止

- 加强 `Esc` / Task cancellation 检查，取消后不会继续启动新的点击动作。
- 如果点击已经按下后才发生取消，App 会继续尝试发送对应的释放事件，再结束该动作。
- 延迟确认等路径在取消后不会继续开启新的确认点击。

### 拖拽释放保护

- 前台拖拽在取消、拖拽事件创建失败或等待中断时，会优先尝试发送 `mouseUp`。
- 降低异常路径下系统保持“鼠标仍按下”状态的风险。

## 保留的 1.1.0 能力

- 窗口定向后台点击与拖拽。
- macOS 版 iPad App 的窗口级激活会话。
- Mission Control / 桌面切换期间的短暂窗口恢复等待。
- 后台虚拟光标。
- 22 个界面、19 个已核查点击动作、7 套流程和 41 个已核查流程节点。

## 安装说明

1. 下载并解压 `GameVisionLab-1.1.1-macOS-arm64.zip`。
2. 将 `GameVisionLab.app` 放入“应用程序”并覆盖旧版。
3. 如 macOS 阻止打开，请在“系统设置 → 隐私与安全性”中选择“仍要打开”。
4. 授予屏幕录制和辅助功能权限后，完全退出并重新打开 App。
5. 先使用“只观察”和“单步点击”确认识别及点击位置，再开始连续运行。

当前安装包为临时签名，尚未经过 Apple 公证。

## 下载文件

- `GameVisionLab-1.1.1-macOS-arm64.zip`
- `GameVisionLab-1.1.1-macOS-arm64.zip.sha256`

SHA-256 请以同一 Release 中随包提供的 `.sha256` 文件为准。
