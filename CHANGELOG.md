# 更新日志

## GameVisionLab 2.0.0 - 2026-08-09

### 百鬼夜行

- 新增百鬼夜行正式流程，可将入口、开始、进行中、结算与奖励页面纳入现有玩法状态机。
- 新增 `Hyakki Vision` 特殊界面识别方式；“百鬼进行中”不依赖 OCR 固定文字，由 Core ML Detector 连续确认目标后进入状态。
- Detector 类型界面与 OCR 分类互斥；百鬼运行期间 OCR 仅用于识别流程明确允许的结算、奖励等退出页面。
- 新增 Detector → Tracker → Gaussian Focus → AIM → LiveClickService 实时投豆链路。
- 投豆冷却支持最短/最长范围，每次投豆后重新随机下一次冷却时间。
- 悬浮监视器在百鬼运行期间自动切换为百鬼实时预览，显示 Track、LOCK/LEARN、Focus、AIM、投豆次数与延迟。

### 配置与发布

- 百鬼实时参数纳入点击动作配置，其他玩法继续沿用原有 OCR/固定区域/动态目标定位方式。
- 新增特殊界面识别配置的发布引导；新安装可以从内置发布配置恢复 Hyakki Vision 类型。
- Release 构建支持内置发布时核查过的 `hyakki_detector` Core ML 模型。
- 修复配置窗口键盘事件监听对普通文本/数字输入造成干扰的问题。
- 发布版本更新为 2.0.0，Build 8。

### 安全与兼容

- 继续保留 `Esc` 紧急停止、窗口安全区域、点击取消保护和后台定向点击机制。
- 当前仍为临时签名、未公证的 Apple Silicon 测试包，要求 macOS 14.0 或更高版本。

## GameVisionLab 1.1.1 - 2026-08-09

### 安全与稳定性修复

- 后台激活引导从合成 `leftMouseDown/leftMouseUp` 改为非点击的定向 `mouseMoved` 事件，避免激活过程额外产生点击语义。
- 加强 `Esc` / 任务取消后的输入保护，取消后不会继续启动新的点击动作。
- 前台点击和后台点击在按下后遇到取消时仍会尽力发送对应的释放事件。
- 前台拖拽在取消、拖拽事件创建失败或等待中断时会优先发送 `mouseUp`，降低鼠标保持按下状态的风险。

### 分发与隐私

- 版本更新为 1.1.1，Build 维持 6。
- 继续使用 22 个界面、19 个已核查点击动作、7 套流程和 41 个已核查流程节点。
- 当前仍为临时签名、未公证的 Apple Silicon 测试包。

## GameVisionLab 1.1.0 - 2026-08-07

### 新增

- 新增窗口定向的后台点击模式，保持视觉前台和真实鼠标不变。
- 新增面向 macOS 版 iPad App 的窗口级激活会话和虚拟点击光标。

### 优化与修复

- 后台激活会话失效后自动重建，并在结束时正确拆除 Event Tap 和恢复窗口激活通知。
- 修复 Event Tap 在普通鼠标或键盘事件上读取 AppKit subtype 导致崩溃的问题。
- Mission Control 期间目标窗口暂时不可见时允许短暂重试，避免一次抖动直接中止任务。
- 自动玩法切换时清除上一流程的过渡校验，支持从战斗中启动并在返回后修正为通用战斗。
- 优化虚拟光标的样式、坐标转换、显示时长和目标窗口层级。

### 分发与隐私

- 内置配置为 22 个界面、19 个已核查点击动作、7 套流程和 41 个已核查流程节点。
- 公开配置已通过引用、核查状态、本机路径和私有字段检查。
- 当前仍为临时签名、未公证的 Apple Silicon 测试包。

## GameVisionLab 1.0.3 - 2026-08-05

### 新增

- 新增整玩法深复制、单界面复制和单点击复制，副本名称自动递增。
- 配置管理新增“重新导入内置配置”，更新内置配置时保留用户配置。

### 优化与修复

- 共享的通用战斗和通用弹窗在复制玩法时继续复用，不生成重复配置。
- 统一界面、点击和流程节点右上角的核查状态入口。
- 悬浮窗统一显示玩法名称，移除不再使用的流程名称字段。
- 补充英杰试炼－秘境重置配置。
- 内置配置更新为 23 个界面、19 个点击动作、7 套流程和 41 个已核查节点。

### 分发与隐私

- 新用户首次启动自动获得本版本内置配置；现有用户可主动重新导入。
- 公开配置已通过本机路径、截图、日志、标注资料和窗口信息检查。
- 当前仍为临时签名、未公证的 Apple Silicon 测试包。

## GameVisionLab 1.0.2 - 2026-08-05

### 新增

- 新增通用战斗玩法，适用于固定入口、重复战斗和结算后再次进入的玩法形式。
- 新增英杰试炼－秘境玩法，支持梦墟秘境、兵藏秘境、战斗结算和自动选择左侧第一个祝福。
- 新增 GitHub Release 更新提示；每天最多检查一次，只提示下载，不自动安装。

### 优化与修复

- 优化配置中心的界面、点击动作和流程管理体验。
- 修复删除自定义界面后可能残留关联动作和无效引用的问题。
- 内置默认配置更新为 22 个界面、18 个点击动作、7 套流程和 40 个已核查节点。

### 分发与隐私

- 公开配置已通过本机路径、截图、日志、标注资料和窗口信息检查。
- 当前仍为临时签名、未公证的 Apple Silicon 测试包。

## GameVisionLab 0.1.1 Beta - 2026-08-02

### 优化

- 优化了配置中心体验。

## GameVisionLab 0.1.0 Beta - 2026-08-02

### 新增

- 引入窗口级截图和 Apple Vision OCR 文字识别。
- 增加 OCR 文字锚点、窗口相对搜索区和窗口相对点击区。
- 增加界面状态确认、点击后画面变化确认和有限重试。
- 增加观察、单步和连续运行模式，以及悬浮实时监控。
- 内置脱敏后的 16 个界面、13 个点击动作、6 套流程和 33 个节点。

### 分发与隐私

- Release 构建去除开发者本机绝对路径和调试符号。
- 包内不包含原始截图、标注库、日志、备份或窗口历史。
- 当前包支持 Apple Silicon 和 macOS 14.0 以上系统。
- 当前仍为临时签名、未公证测试包。

## 3.0 - 2026-08-01

### 新增

- 三模式选择页：区域随机点击、画面识别回合、动态探索副本。
- 每个模式统一为“选择模式 → 配置并测试 → 开始运行”的流程。
- 固定回合与探索方案切换、新建、复制、重命名和删除。
- 关键运行时间、停止条件和执行摘要直接展示。
- 动态探索八项配置清单、地图预览和圆形战斗目标识别。
- 全局诊断日志入口、实时跟随、元数据和手动刷新。
- 所有模式共用 `Esc` 紧急停止。

### 调整

- 圆形战斗目标的有效最低置信度提升并固定为不低于 `0.85`。
- 普通/首领战斗改为持续读取状态，等待时间作为最早检查或异常保护参数。
- 公开包不包含个人模板、配置 UUID、运行日志、窗口状态、旧压缩包和开发缓存。
- 版本号更新为 `3.0`，构建号更新为 `30`。

### 分发说明

- 当前候选为临时签名，未进行 Apple Developer ID 签名和公证。
- Apple Silicon 已验证；Intel 构建路径保留但未实机验证。
